#!/bin/bash
# Single verification entrypoint. AGENTS.md and any CI defer to this file,
# so the gate lives in exactly one place.
set -euo pipefail
cd "$(dirname "$0")/.."

npm run typecheck
npm test
npm run build
